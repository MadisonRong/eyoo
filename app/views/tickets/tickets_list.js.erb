$(".main-content").html("")
$(".main-content").html("<%= escape_javascript(render partial: 'shared/list', object: @title) %>")
$(".main-content").append("<%= escape_javascript(render('shared/dialog')) %>")
$(".main-content").append("<script src='assets/ajaxfileupload.js'></script>")

			
			jQuery(function($) {
				var grid_selector = "#grid-table";
				var pager_selector = "#grid-pager";
			
				jQuery(grid_selector).jqGrid({
					url: '<%= admins_tickets_json_list_url %>',
					postData: {"type" : "1"},
					mType: 'get',
					datatype: 'json',
					height: '100%',
					autowidth: true,
					loadui: 'block',
					editurl: '<%= admins_tickets_update_path %>',
					caption: "管理商品",
					colNames:['id','门票名称','价格','景区','省份','城市','描述','状态','门票类型','创建时间','图片'],
					colModel:[
						{name:'id',index:'id', width:60, sorttype:"int", editable: false, sortable: true},
						{name:'name',index:'name',width:90, editable:true,editoptions:{size:"20",maxlength:"30"}, sortable: true},
						{name:'price',index:'price', width:60, sorttype:"float", editable: true, sortable: true},
						{name:'scenic',index:'scenic',width:90, editable:true, sortable: true,
						edittype:"select",editoptions:{
							size:"20",maxlength:"30",
							dataUrl:"<%= admins_scenics_select_path %>",
							buildSelect: function (data) {
								// console.log(data);
								data=JSON.parse(data);
						             var s = "<select>";
						             if (data != null) {
						                 s += '<option value="' + data.key + '">' +
						                             data.value + '</option>';
						             }
						             return s + "</select>";
						        }
					        },
					        ajaxSelectOptions: { dataType: "json" }
						},
						{name:'province',index:'province',width:90, editable:true, sortable: true,
						formatter:function(cellvalue, options, rowObject){
							// console.log("cellvalue:"+cellvalue)
							// console.log(options)
							// console.log("rowObject"+rowObject)
							// console.log("province name:"+rowObject.province_name)
							return rowObject.province_name
						},
						edittype:"select",editoptions:{
							size:"20",maxlength:"30",
							dataUrl:"<%= admins_region_provinces_path %>",
							buildSelect: function (data) {
								// console.log(data);
								data=JSON.parse(data);
						             var s = "<select>";
						             if (data != null && data.length) {
						                 for (var i = 0; i < data.length; i++) {
						                 	item = data[i];
						                 	s += '<option value="' + item.id + '">' +
						                             item.name + '</option>';
						                 };
						             }
						             return s + "</select>";
						        }
					        },
					        ajaxSelectOptions: { dataType: "json" }
						},
						{name:'city',index:'city',width:90, editable:true, sortable: true,
						formatter:function(cellvalue, options, rowObject){
							return rowObject.city_name
						},
						edittype:"select",editoptions:{
							size:"20",maxlength:"30",
							dataUrl:"<%= admins_region_cities_path %>",
							buildSelect: function (data) {
								// console.log(data);
								data=JSON.parse(data);
						             var s = "<select>",i;
						             if (data != null && data.length) {
						                 for (var i = 0; i < data.length; i++) {
						                 	item = data[i];
						                 	s += '<option value="' + item.id + '">' +
						                             item.name + '</option>';
						                 };
						             }
						             return s + "</select>";
						        }
					        },
					        ajaxSelectOptions: { dataType: "json" }
						},
						{name:'description',index:'description',width:90, editable:true,editoptions:{size:"20",maxlength:"30"}, sortable: true},
						{name:'status',index:'status', width:60, sorttype:"int", editable: false, sortable: true,
					    formatter:function(value){
								if (value==0) { return '未通过'; }
								if (value==1) { return '已通过'; }
							}},
						{name:'ticket_type_id',index:'ticket_type_id', width:60, sorttype:"int", 
						  editable: true, sortable: true, 
							formatter:function(value){
								if (value==1) { return '漂流'; }
								if (value==2) { return '滑雪'; }
								if (value==3) { return '游乐场'; }
								if (value==4) { return '温泉'; }
								if (value==5) { return '名山胜水'; }
								if (value==6) { return '古镇'; };
							},
							edittype:"select",editoptions:{
							size:"20",maxlength:"30",
							dataUrl:"<%= admins_tickets_type_path %>",
							buildSelect: function (data) {
								data=JSON.parse(data);
						             var s = "<select>", i, l, item;
						             if (data != null && data.length) {
						                 for (i = 0, l = data.length; i < l; i++) {
						                     item = data[i];
						                     s += '<option value="' + item.key + '">' +
						                             item.value + '</option>';
						                 }
						             }
						             return s + "</select>";
						        }
					        },
					    ajaxSelectOptions: { dataType: "json" }
						},
						{name:'created_at',index:'created_at', width:150,editable: false, sorttype:"date",unformat: pickDate, sortable: true},
						{name:'picture',index:'picture', width:60, editable: true, sortable: true,
						 hidden: true, edittype: "image",
							editoptions: {size:"20",maxlength:"30",src: ""},
							editrules: {edithidden: true}},
					], 
			
					viewrecords : true,
					rowNum:10,
					rowList:[10,20,30],
					pager : pager_selector,
					altRows: true,
					//toppager: true,
					
					multiselect: false,
					//multikey: "ctrlKey",
			        multiboxonly: true,
					loadError : function(xhr, status, error){
						console.log(status + " loading data of " + $(this).attr("id") + " : " + error)
					},
					loadComplete : function() {
						var table = this;
						setTimeout(function(){
							styleCheckbox(table);
							
							updateActionIcons(table);
							updatePagerIcons(table);
							enableTooltips(table);
						}, 0);
					}
				});
			
				//enable search/filter toolbar
				//jQuery(grid_selector).jqGrid('filterToolbar',{defaultSearch:true,stringResult:true})
			
				//switch element when editing inline
				function aceSwitch( cellvalue, options, cell ) {
					setTimeout(function(){
						$(cell) .find('input[type=checkbox]')
								.wrap('<label class="inline" />')
							.addClass('ace ace-switch ace-switch-5')
							.after('<span class="lbl"></span>');
					}, 0);
				}
				//enable datepicker
				function pickDate( cellvalue, options, cell ) {
					setTimeout(function(){
						$(cell) .find('input[type=text]')
								.datepicker({format:'yyyy-mm-dd' , autoclose:true}); 
					}, 0);
				}
			
			
				//navButtons
				jQuery(grid_selector).jqGrid('navGrid',pager_selector,
					{ 	//navbar options
						edit: true,
						editicon : 'icon-pencil blue',
						add: true,
						addicon : 'icon-plus-sign purple',
						del: true,
						delicon : 'icon-trash red',
						search: false,
						searchicon : 'icon-search orange',
						refresh: true,
						refreshicon : 'icon-refresh green',
						view: true,
						viewicon : 'icon-zoom-in grey',
					},
					{
						//edit record form
						closeAfterEdit: true,
						recreateForm: true,
						beforeShowForm : function(e) {
							var form = $(e[0]);
							form.closest('.ui-jqdialog').find('.ui-jqdialog-titlebar').wrapInner('<div class="widget-header" />')
							style_edit_form(form);
					    $("#picture").after("<%= escape_javascript(render('shared/upload_image_form')) %>");
					    $("#picture").click(function(){
					    	console.log('click on image...');
					    	$("#file").click();
					    });
						},
						afterShowForm : function(e){
					    console.log($("#province"));
					    console.log($("#city"));
					    console.log("after show form.....");
							$("#province").removeClass("FormElement");
							$("#city").removeClass("FormElement");
						},
						beforeInitData: function (e) {
              var $self = $(this);

              //operating_license
              cm = $self.jqGrid("getColProp", "picture");
              selRowId = $self.jqGrid("getGridParam", "selrow");
              cellvalue=$self.jqGrid("getCell", selRowId, 10);
              cm.editoptions.src = cellvalue;
            }
					},
					{
						//new record form
						closeAfterAdd: true,
						recreateForm: true,
						viewPagerButtons: false,
						beforeShowForm : function(e) {
							var form = $(e[0]);
							form.closest('.ui-jqdialog').find('.ui-jqdialog-titlebar').wrapInner('<div class="widget-header" />')
							style_edit_form(form);
							$("#province").removeClass("FormElement");
							$("#city").removeClass("FormElement");
						}
					},
					{
						//delete record form
						recreateForm: true,
						beforeShowForm : function(e) {
							var form = $(e[0]);
							if(form.data('styled')) return false;
							
							form.closest('.ui-jqdialog').find('.ui-jqdialog-titlebar').wrapInner('<div class="widget-header" />')
							style_delete_form(form);
							
							form.data('styled', true);
						},
						onClick : function(e) {
							alert(1);
						}
					},
					{
						//search form
						recreateForm: true,
						afterShowSearch: function(e){
							var form = $(e[0]);
							form.closest('.ui-jqdialog').find('.ui-jqdialog-title').wrap('<div class="widget-header" />')
							style_search_form(form);
						},
						afterRedraw: function(){
							style_search_filters($(this));
						}
						,
						multipleSearch: true,
						/**
						multipleGroup:true,
						showQuery: true
						*/
					},
					{
						//view record form
						recreateForm: true,
						beforeShowForm: function(e){
							var form = $(e[0]);
							form.closest('.ui-jqdialog').find('.ui-jqdialog-title').wrap('<div class="widget-header" />')
							var v_picture_value = $("#v_picture > span").html();
							$("#v_picture > span").html("<img src=\""+v_picture_value+"\"/>");
						}
					}
				)
			
			
				
				function style_edit_form(form) {
					//enable datepicker on "sdate" field and switches for "stock" field
					form.find('input[name=sdate]').datepicker({format:'yyyy-mm-dd' , autoclose:true})
						.end().find('input[name=stock]')
							  .addClass('ace ace-switch ace-switch-5').wrap('<label class="inline" />').after('<span class="lbl"></span>');
			
					//update buttons classes
					var buttons = form.next().find('.EditButton .fm-button');
					buttons.addClass('btn btn-sm').find('[class*="-icon"]').remove();//ui-icon, s-icon
					buttons.eq(0).addClass('btn-primary').prepend('<i class="icon-ok"></i>');
					buttons.eq(1).prepend('<i class="icon-remove"></i>')
					
					buttons = form.next().find('.navButton a');
					buttons.find('.ui-icon').remove();
					buttons.eq(0).append('<i class="icon-chevron-left"></i>');
					buttons.eq(1).append('<i class="icon-chevron-right"></i>');		


				}
			
				function style_delete_form(form) {
					var buttons = form.next().find('.EditButton .fm-button');
					buttons.addClass('btn btn-sm').find('[class*="-icon"]').remove();//ui-icon, s-icon
					buttons.eq(0).addClass('btn-danger').prepend('<i class="icon-trash"></i>');
					buttons.eq(1).prepend('<i class="icon-remove"></i>');
					$('.ui-jqdialog-title').eq(0).html("审核商品");
					$('.delmsg').html("你确定要通过审核吗？");
					console.log($('#dData'));
					$('#dData').html("<i class=\"icon-trash\"></i>Yes");
					$('#eData').html("<i class=\"icon-remove\"></i>No");
				}
				
				function style_search_filters(form) {
					form.find('.delete-rule').val('X');
					form.find('.add-rule').addClass('btn btn-xs btn-primary');
					form.find('.add-group').addClass('btn btn-xs btn-success');
					form.find('.delete-group').addClass('btn btn-xs btn-danger');
				}
				function style_search_form(form) {
					var dialog = form.closest('.ui-jqdialog');
					var buttons = dialog.find('.EditTable')
					buttons.find('.EditButton a[id*="_reset"]').addClass('btn btn-sm btn-info').find('.ui-icon').attr('class', 'icon-retweet');
					buttons.find('.EditButton a[id*="_query"]').addClass('btn btn-sm btn-inverse').find('.ui-icon').attr('class', 'icon-comment-alt');
					buttons.find('.EditButton a[id*="_search"]').addClass('btn btn-sm btn-purple').find('.ui-icon').attr('class', 'icon-search');
				}
				
				function beforeDeleteCallback(e) {
					var form = $(e[0]);
					if(form.data('styled')) return false;
					
					form.closest('.ui-jqdialog').find('.ui-jqdialog-titlebar').wrapInner('<div class="widget-header" />')
					style_delete_form(form);
					
					form.data('styled', true);
				}
				
				function beforeEditCallback(e) {
					var form = $(e[0]);
					form.closest('.ui-jqdialog').find('.ui-jqdialog-titlebar').wrapInner('<div class="widget-header" />')
					style_edit_form(form);
				}
			
			
			
				//it causes some flicker when reloading or navigating grid
				//it may be possible to have some custom formatter to do this as the grid is being created to prevent this
				//or go back to default browser checkbox styles for the grid
				function styleCheckbox(table) {
				/**
					$(table).find('input:checkbox').addClass('ace')
					.wrap('<label />')
					.after('<span class="lbl align-top" />')
			
			
					$('.ui-jqgrid-labels th[id*="_cb"]:first-child')
					.find('input.cbox[type=checkbox]').addClass('ace')
					.wrap('<label />').after('<span class="lbl align-top" />');
				*/
				}
				
			
				//unlike navButtons icons, action icons in rows seem to be hard-coded
				//you can change them like this in here if you want
				function updateActionIcons(table) {
					/**
					var replacement = 
					{
						'ui-icon-pencil' : 'icon-pencil blue',
						'ui-icon-trash' : 'icon-trash red',
						'ui-icon-disk' : 'icon-ok green',
						'ui-icon-cancel' : 'icon-remove red'
					};
					$(table).find('.ui-pg-div span.ui-icon').each(function(){
						var icon = $(this);
						var $class = $.trim(icon.attr('class').replace('ui-icon', ''));
						if($class in replacement) icon.attr('class', 'ui-icon '+replacement[$class]);
					})
					*/
				}
				
				//replace icons with FontAwesome icons like above
				function updatePagerIcons(table) {
					var replacement = 
					{
						'ui-icon-seek-first' : 'icon-double-angle-left bigger-140',
						'ui-icon-seek-prev' : 'icon-angle-left bigger-140',
						'ui-icon-seek-next' : 'icon-angle-right bigger-140',
						'ui-icon-seek-end' : 'icon-double-angle-right bigger-140'
					};
					$('.ui-pg-table:not(.navtable) > tbody > tr > .ui-pg-button > .ui-icon').each(function(){
						var icon = $(this);
						var $class = $.trim(icon.attr('class').replace('ui-icon', ''));
						
						if($class in replacement) icon.attr('class', 'ui-icon '+replacement[$class]);
					})
				}
			
				function enableTooltips(table) {
					$('.navtable .ui-pg-button').tooltip({container:'body'});
					$(table).find('.ui-pg-div').tooltip({container:'body'});
				}
			
				//var selr = jQuery(grid_selector).jqGrid('getGridParam','selrow');
			
			
			});